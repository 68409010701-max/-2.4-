<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>ตัวอย่าง HTML + CSS</title>
    <style>
        body {
            background-color: #f0f4f8;      /* CSS อย่างที่ 1 */
            font-family: Arial, sans-serif; /* CSS อย่างที่ 2 */
            display: flex;                  /* CSS อย่างที่ 3 */
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .card {
            background-color: white;
            padding: 20px;
            border-radius: 12px;            /* CSS อย่างที่ 4 */
            box-shadow: 0 4px 10px rgba(0,0,0,0.1); /* CSS อย่างที่ 5 */
            text-align: center;
        }

        .card h1 {
            color: #0077cc;                 /* CSS อย่างที่ 6 */
        }

        .card button {
            background-color: #0077cc;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 6px;
            cursor: pointer;
        }

        .card button:hover {
            background-color: #005fa3;      /* CSS อย่างที่ 7 */
        }
    </style>
</head>
<body>

    <div class="card">
        <h1>สวัสดี HTML & CSS</h1>
        <p>นี่คือตัวอย่างหน้าเว็บแบบง่าย ๆ</p>
        <button>กดที่นี่</button>
    </div>

</body>
</html>
